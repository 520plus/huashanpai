package com.example.springsecurity.util;

public class SpringSecurityConfig {
  /*  protected void configure(HttpSecurity http) throws Exception {
        http
                .csrf().disable()
                .authorizeRequests()
                .anyRequest().authenticated()//所有请求必须登陆后访问定义哪些URL需要被保护、哪些不需要被保护
                .and()
                .formLogin()
                .loginPage("/login")
                .login
                .failureUrl("/login?error")
                .permitAll()//登录界面，错误界面可以直接访问
                .and()
                .logout()
                .permitAll();//注销请求可直接访问
               // 定义当需要用户登录时候，转到的登录页面。  http.formLogin()




    }
*/

}
